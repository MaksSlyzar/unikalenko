class BuildObject {
    posX: number;
    posY: number;
    width: number;
    height: number;
    cliteWidth: number;
    cliteHeight: number;

    imageProps: {
        x: number,
        y: number,
        width: number,
        height: number
    };

    draw () {};
    builded () {};
    update() {};
}

export default BuildObject;