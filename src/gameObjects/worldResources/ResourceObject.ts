class ResourceObject {
    posX: number;
    posY: number;
    width: number;
    height: number;
    cliteWidth: number;
    cliteHeight: number;
    
    imageProps: {
        x: number,
        y: number,
        width: number,
        height: number
    };

    draw () {};
}

export default ResourceObject;